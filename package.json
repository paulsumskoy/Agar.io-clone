{ 
  "scripts": {
    "test": "echo \\\"Error: no test specified\\\" && exit 1",
    "start": "cd multiplayer && node server.js",
    "heroku-postbuild": "npm run build"
  },
  "dependencies": {
    "express": "^4.17.1",
    "socket.io": "^4.4.0"
  }
}
